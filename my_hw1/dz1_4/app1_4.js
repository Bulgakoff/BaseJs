'use strict';

// defer - скрипт  Выполняется после того, 
// как документ загружен и обработан(ждёт), непосредственно перед DOMContentLoaded.

// async - порядок загрузки не имеет значения . скрипт Может загрузиться и выполниться до того,
// как страница полностью загрузится.
// Такое случается, если скрипты маленькие или хранятся в кеше, а документ достаточно большой.